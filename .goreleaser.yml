# yaml-language-server: $schema=https://goreleaser.com/static/schema.json
version: 2

project_name: go-branchy

force_token: github

before:
  hooks:
    - go mod tidy

builds:
  - binary: "{{ .ProjectName }}"
    main: ./cmd/go-branchy
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
      - linux
    goarch:
      - arm64
      - amd64
    ldflags:
      - -s -w
      - -X github.com/untcha/go-branchy/internal/appmeta.Version={{.Version}}
      - -X github.com/untcha/go-branchy/internal/appmeta.Commit={{.ShortCommit}}
      - -X github.com/untcha/go-branchy/internal/appmeta.BuildDate={{.Date}}

archives:
  - formats:
      - tar.gz
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

checksum:
  name_template: "{{ .ProjectName }}_{{ .Version }}_checksums.txt"
  algorithm: sha256

snapshot:
  version_template: "{{ incpatch .Version }}-dev"

changelog:
  sort: asc
  use: github
  filters:
    exclude:
      # - "^docs:"
      # - "^test:"
      - "merge conflict"
      - Merge pull request
      - Merge remote-tracking branch
      - Merge branch
      - go mod tidy
  groups:
    - title: Dependency updates
      regexp: '^(feat|fix)( .*)?\(deps\)!?:.+$'
      order: 50
    - title: "New Features"
      regexp: '^feat( .*)?(\([[:word:]]+\))??!?:.+$'
      order: 100
    # - title: "Security updates"
    #   regexp: '^.*?sec(\([[:word:]]+\))??!?:.+$'
    #   order: 150
    - title: "Bug fixes"
      regexp: '^fix( .*)?(\([[:word:]]+\))??!?:.+$'
      order: 200
    - title: "Documentation updates"
      regexp: '^docs( .*)?(\([[:word:]]+\))??!?:.+$'
      order: 400
    - title: "Build process updates"
      regexp: '^build( .*)?(\([[:word:]]+\))??!?:.+$'
      order: 400
    # chore, refactor, test
    - title: Other work
      order: 9999

release:
  footer: |
    **Full Changelog**: https://github.com/untcha/go-branchy/compare/{{ .PreviousTag }}...{{ .Tag }}

# yaml-language-server: $schema=https://goreleaser.com/static/schema.json
# vim: set ts=2 sw=2 tw=0 fo=jcroql
